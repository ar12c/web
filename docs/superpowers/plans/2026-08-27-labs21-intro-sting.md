{
  "filePath": "/Users/ar12c/Desktop/web/docs/superpowers/plans/2026-08-27-labs21-intro-sting.md",
  "content": "# Labs21 Fullscreen Intro Sting — Implementation Plan\n\n> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.\n\n**Goal:** Add a fullscreen black overlay to `AI/index.html` that plays a ~500ms rapid-cut sting of the 3D Labs21 logo sequence, then fades out to reveal the landing page. Plays once per browsing session.\n\n**Architecture:** Encode a 48-frame sting mp4 from every 6th frame of the existing hero video, played at 96fps. A fullscreen `<div>` with inline styles covers the page, autoplays the sting on load, fades out on `ended`, and sets `sessionStorage` to skip on subsequent visits.\n\n**Tech Stack:** ffmpeg for encoding, vanilla HTML/CSS/JS with inline styles, `sessionStorage`.\n\n---\n\n### Task 1: Encode the sting video\n\n**Files:**\n- Create: `AI/video/labs21-sting.mp4`\n\nSource: `AI/video/labs21-hero.mp4` (288 frames, 30fps, 1920×1080)\n\n- [ ] **Step 1: Run ffmpeg to extract every 6th frame at 96fps**\n\n```bash\nffmpeg -i AI/video/labs21-hero.mp4 \\\n  -vf \"select='not(mod(n,6))',setpts=N/FRAME_RATE/TB\" \\\n  -r 96 -c:v libx264 -preset fast -crf 18 \\\n  -pix_fmt yuv420p -movflags +faststart \\\n  AI/video/labs21-sting.mp4\n```\n\n- [ ] **Step 2: Verify the output**\n\n```bash\nls -lh AI/video/labs21-sting.mp4 && ffprobe -v quiet -show_entries format=duration -of csv=p=0 AI/video/labs21-sting.mp4\n```\n\nExpected: file exists, duration ~0.5s, size 200–500KB.\n\n---\n\n### Task 2: Add intro overlay to AI/index.html\n\n**Files:**\n- Modify: `AI/index.html` (insert overlay + script at top of `<body>`)\n\nRead `AI/index.html` to find the opening `<body>` tag. Insert the overlay div and inline script after `<body>` and before the hero section.\n\n- [ ] **Step 1: Insert the overlay HTML**\n\nAfter `<body data-page=\"ai-home\" class=\"...\">`, insert:\n\n```html\n\n    <div id=\"intro-overlay\" style=\"position:fixed;inset:0;z-index:9999;background:#000;display:flex;align-items:center;justify-content:center\">\n        <video id=\"intro-video\"\n               src=\"video/labs21-sting.mp4\"\n               autoplay muted playsinline\n               style=\"width:100%;height:100%;object-fit:cover\">\n        </video>\n    </div>\n```\n\n- [ ] **Step 2: Insert the intro script**\n\nImmediately after the overlay div:\n\n```html\n    <script>\n        (function () {\n            var overlay = document.getElementById('intro-overlay');\n            if (!overlay) return;\n\n            if (sessionStorage.getItem('labs21_intro_seen') === '1') { overlay.remove(); return; }\n            if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) { overlay.remove(); return; }\n\n            var video = document.getElementById('intro-video');\n            var done = false;\n\n            function dismiss() {\n                if (done) return;\n                done = true;\n                sessionStorage.setItem('labs21_intro_seen', '1');\n                overlay.style.transition = 'opacity 0.3s var(--ease-smooth)';\n                overlay.style.opacity = '0';\n                overlay.addEventListener('transitionend', function () { overlay.remove(); });\n            }\n\n            if (video) {\n                video.addEventListener('ended', dismiss);\n            }\n\n            setTimeout(dismiss, 800);\n        })();\n    </script>\n```\n\n- [ ] **Step 3: Verify the page structure**\n\nConfirm the body content order is:\n1. `#intro-overlay` div\n2. Intro `<script>`\n3. Existing hero `<section class=\"hero-section\">`\n4. Everything else (remaining sections, NAV_CONFIG, motion.js, nav.js)\n\n---\n\n### Task 3: Verify\n\n- [ ] **Step 1: Run existing tests**\n\n```bash\nnode test-z-index.mjs && node test-ai-home-theme.mjs\n```\n\nExpected: all pass.\n\n- [ ] **Step 2: Verify the sting video plays in browser**\n\nOpen `AI/index.html`. Expected:\n- Fullscreen black overlay appears on load\n- Sting video plays (~0.5s)\n- Overlay fades out (~0.3s)\n- Landing page visible\n- Reload: no overlay (session flag set)\n- Close tab, reopen: overlay plays again (session storage cleared)\n\n- [ ] **Step 3: Verify reduced motion**\n\nEnable `prefers-reduced-motion: reduce` in DevTools. Reload. Expected: no overlay, page shows immediately.\n\n- [ ] **Step 4: Verify fallback timeout**\n\nBlock the sting video from loading (DevTools → Network → block). Reload. Expected: overlay removed after ~800ms.\n"
}
