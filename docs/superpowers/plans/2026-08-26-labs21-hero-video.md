{
  "filePath": "/Users/ar12c/Desktop/web/docs/superpowers/plans/2026-08-26-labs21-hero-video.md",
  "content": "# Labs21 3D Logo Hero Video — Implementation Plan\n\n> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.\n\n**Goal:** Replace the two-column storybook hero on `AI/index.html` with a full-width auto-playing mp4 video of the 288-frame 3D Labs21 logo sequence, with text overlays that fade in at choreographed timestamps.\n\n**Architecture:** 288 PNGs → ffmpeg → `AI/video/labs21-hero.mp4` (~15MB). `<video autoplay muted playsinline>` as hero background with `object-fit: cover`. Text reveals (\"Stay\", \"Curious.\", Start button) fire via `timeupdate` at 2s / 4.5s / 6.5s. Replay button appears after video ends. Existing page content (Based AI, BDH Neural) unchanged.\n\n**Tech Stack:** ffmpeg for encoding, vanilla HTML/CSS/JS, Tailwind CDN, existing `src/site.css` token system.\n\n---\n\n### Task 1: Encode PNG sequence to mp4 video\n\n**Files:**\n- Create: `AI/video/labs21-hero.mp4`\n\nSource: `/Users/ar12c/Desktop/Labs21/0001.png` through `0288.png` (288 frames, 1920×1080, RGBA)\n\n- [ ] **Step 1: Create video directory**\n\n```bash\nmkdir -p AI/video\n```\n\n- [ ] **Step 2: Run ffmpeg to encode the PNG sequence**\n\n```bash\nffmpeg -framerate 30 \\\n  -i /Users/ar12c/Desktop/Labs21/%04d.png \\\n  -c:v libx264 -preset slow -crf 23 \\\n  -pix_fmt yuv420p \\\n  -movflags +faststart \\\n  AI/video/labs21-hero.mp4\n```\n\nExpected: output file `AI/video/labs21-hero.mp4`, ~15–25MB, ~9.6s duration.\n\n- [ ] **Step 3: Verify the output**\n\n```bash\nls -lh AI/video/labs21-hero.mp4 && ffprobe -v quiet -show_entries format=duration,size -of csv=p=0 AI/video/labs21-hero.mp4\n```\n\nExpected: file exists, duration ~9.6s, size 15–25MB.\n\n---\n\n### Task 2: Update CSS — remove storybook styles, add hero video styles\n\n**Files:**\n- Modify: `src/site.css` (remove lines 1150–1409, line 1515, line 1519; add new hero styles after the apple-color-cycle block at ~line 1444)\n\n- [ ] **Step 1: Remove storybook CSS block (lines 1150–1409)**\n\nDelete from the comment `/* --- Flipping Storybook --- */` through the closing `}` of the `@media (prefers-reduced-motion)` block.\n\nThe exact old string to remove:\n\n```css\n\n        /* --- Flipping Storybook --- */\n        /* Open book with a leather cover, stacked page edges and a bookmark\n           ribbon. The right leaf turns over the spine in 3D via the Web\n           Animations API (script at the bottom): JS owns the whole cycle so\n           the word swap and the leaf reset land in the same paint — no\n           event-timing flashes. The leaf also rests slightly ABOVE the page\n           plane (translateZ) so coplanar z-fighting can never flicker. */\n        [data-page=\"ai-home\"] .book-scene {\n            position: relative;\n            width: 100%;\n            display: flex;\n            flex-direction: column;\n            align-items: center;\n            justify-content: center;\n            pointer-events: none;\n        }\n\n        [data-page=\"ai-home\"] .book-wrap {\n            width: 100%;\n            display: flex;\n            justify-content: center;\n            perspective: 1300px;\n            animation: book-bob 4.5s ease-in-out infinite;\n        }\n\n        [data-page=\"ai-home\"] .book {\n            position: relative;\n            width: min(82%, 440px);\n            aspect-ratio: 5 / 3.2;\n            transform: rotateX(7deg);\n            transform-style: preserve-3d;\n            --paper: color-mix(in srgb, var(--bg-elevated), white 55%);\n            --paper-2: color-mix(in srgb, var(--bg-elevated), black 5%);\n            --paper-edge: color-mix(in srgb, var(--accent) 22%, var(--bg-elevated));\n            --rule: color-mix(in srgb, var(--accent) 11%, transparent);\n            --rule-lines: repeating-linear-gradient(to bottom,\n                    transparent 0 1.15em,\n                    var(--rule) 1.15em calc(1.15em + 1px));\n        }\n\n        .dark [data-page=\"ai-home\"] .book {\n            --paper: color-mix(in srgb, var(--bg-elevated), white 8%);\n            --paper-2: color-mix(in srgb, var(--bg-elevated), white 3%);\n            --paper-edge: color-mix(in srgb, var(--accent) 20%, var(--bg-elevated));\n            --rule: color-mix(in srgb, var(--accent) 16%, transparent);\n        }\n\n        /* Leather cover peeking out around the page block, with a spine\n           crease down the middle and a darker board edge at the bottom */\n        [data-page=\"ai-home\"] .book-cover {\n            position: absolute;\n            inset: -3.5% -2.8% -8.5% -2.8%;\n            border-radius: 12px;\n            background:\n                linear-gradient(90deg, transparent 45%, rgba(0, 0, 0, 0.28) 50%, transparent 55%),\n                linear-gradient(180deg,\n                    color-mix(in srgb, var(--accent), black 10%),\n                    color-mix(in srgb, var(--accent), black 38%));\n            box-shadow: 0 26px 36px -22px rgba(0, 0, 0, 0.55),\n                inset 0 1px 0 rgba(255, 255, 255, 0.2),\n                inset 0 -6px 0 -3px rgba(0, 0, 0, 0.25);\n        }\n\n        /* Bookmark ribbon hanging out of the gutter, over the bottom cover */\n        [data-page=\"ai-home\"] .book-ribbon {\n            position: absolute;\n            left: 50%;\n            bottom: -10.5%;\n            width: 4.5%;\n            height: 17%;\n            transform: translateX(-50%) rotate(2.5deg);\n            background: linear-gradient(180deg,\n                    color-mix(in srgb, var(--accent), black 48%),\n                    color-mix(in srgb, var(--accent), black 22%));\n            clip-path: polygon(0 0, 100% 0, 100% 100%, 50% 82%, 0 100%);\n            filter: drop-shadow(0 3px 2px rgba(0, 0, 0, 0.35));\n        }\n\n        [data-page=\"ai-home\"] .book-page,\n[data-page=\"ai-home\"] .leaf-face {\n            background-color: var(--paper);\n            /* layer 1: paper curving down into the gutter (overridden per\n               side below); layer 2: faint ruled lines so the pages read as\n               written-in */\n            background-image:\n                linear-gradient(to right, transparent, transparent),\n                var(--rule-lines);\n            background-repeat: no-repeat, no-repeat;\n            background-size: 100% 100%, 74% 78%;\n            background-position: 0 0, center 65%;\n            border: 1px solid color-mix(in srgb, var(--accent) 22%, transparent);\n            display: flex;\n            flex-direction: column;\n            align-items: center;\n            justify-content: center;\n            gap: 0.3em;\n            padding: 7% 5%;\n            overflow: hidden;\n            text-align: center;\n        }\n\n        [data-page=\"ai-home\"] .book-page {\n            position: absolute;\n            top: 0;\n            height: 100%;\n            width: 50%;\n        }\n\n        /* Stacked paper edges below the spread give the block thickness */\n        [data-page=\"ai-home\"] .book-page--left {\n            left: 0;\n            border-radius: 8px 2px 2px 8px;\n            box-shadow: inset -16px 0 18px -14px rgba(0, 0, 0, 0.28),\n                0 2px 0 0 var(--paper-2),\n                0 4px 0 0 var(--paper-edge),\n                0 6px 0 -1px var(--paper-2),\n                0 8px 0 -1px var(--paper-edge);\n        }\n\n        [data-page=\"ai-home\"] .book-page--right {\n            right: 0;\n            border-radius: 2px 8px 8px 2px;\n            box-shadow: inset 16px 0 18px -14px rgba(0, 0, 0, 0.28),\n                0 2px 0 0 var(--paper-2),\n                0 4px 0 0 var(--paper-edge),\n                0 6px 0 -1px var(--paper-2),\n                0 8px 0 -1px var(--paper-edge);\n        }\n\n        [data-page=\"ai-home\"] .book-leaf {\n            position: absolute;\n            left: 50%;\n            top: 0;\n            width: 50%;\n            height: 100%;\n            transform-origin: left center;\n            transform-style: preserve-3d;\n            /* rest a hair above the page plane — never coplanar, never\n               z-fights with the page it covers */\n            transform: translateZ(0.8px);\n            z-index: 2;\n        }\n\n        [data-page=\"ai-home\"] .leaf-face {\n            position: absolute;\n            inset: 0;\n            backface-visibility: hidden;\n            -webkit-backface-visibility: hidden;\n        }\n\n        [data-page=\"ai-home\"] .leaf-face--front {\n            border-radius: 2px 8px 8px 2px;\n            box-shadow: inset 16px 0 18px -14px rgba(0, 0, 0, 0.28);\n        }\n\n        [data-page=\"ai-home\"] .leaf-face--back {\n            transform: rotateY(180deg);\n            border-radius: 8px 2px 2px 8px;\n            box-shadow: inset -16px 0 18px -14px rgba(0, 0, 0, 0.28);\n        }\n\n        /* Paper curves down into the gutter on the spine side of each page.\n           Right-hand surfaces (base right page + leaf front) shade left;\n           left-hand surfaces (base left page + leaf back) shade right. */\n        [data-page=\"ai-home\"] .book-page--left,\n[data-page=\"ai-home\"] .leaf-face--back {\n            background-image:\n                linear-gradient(to right, transparent 72%, rgba(0, 0, 0, 0.05) 90%, rgba(0, 0, 0, 0.14) 100%),\n                var(--rule-lines);\n        }\n\n        [data-page=\"ai-home\"] .book-page--right,\n[data-page=\"ai-home\"] .leaf-face--front {\n            background-image:\n                linear-gradient(to left, transparent 72%, rgba(0, 0, 0, 0.05) 90%, rgba(0, 0, 0, 0.14) 100%),\n                var(--rule-lines);\n        }\n\n        /* Soft floor shadow so the floating book sits on something */\n        [data-page=\"ai-home\"] .book-floor-shadow {\n            width: 46%;\n            max-width: 250px;\n            height: 20px;\n            margin-top: 9%;\n            border-radius: 50%;\n            background: radial-gradient(ellipse at center, rgba(0, 0, 0, 0.3), transparent 68%);\n            animation: book-floor 4.5s ease-in-out infinite;\n        }\n\n        /* Page typography — the fun part */\n        [data-page=\"ai-home\"] .page-word {\n            font-family: 'Century', Georgia, serif;\n            font-style: italic;\n            font-weight: 700;\n            font-size: clamp(1.1rem, 3.4vw, 1.55rem);\n            color: var(--accent);\n            transform: rotate(-4deg);\n            text-decoration: underline;\n            text-decoration-style: wavy;\n            text-decoration-color: color-mix(in srgb, var(--accent) 50%, transparent);\n            text-decoration-thickness: 1.5px;\n            text-underline-offset: 0.22em;\n            white-space: nowrap;\n        }\n\n        [data-page=\"ai-home\"] .page-line {\n            font-size: clamp(0.58rem, 1.6vw, 0.76rem);\n            font-style: italic;\n            line-height: 1.6;\n            color: var(--text-secondary);\n            white-space: nowrap;\n        }\n\n        [data-page=\"ai-home\"] .page-caption {\n            font-size: clamp(0.52rem, 1.4vw, 0.66rem);\n            letter-spacing: 0.2em;\n            text-transform: uppercase;\n            color: var(--text-tertiary);\n        }\n\n        [data-page=\"ai-home\"] .page-doodle {\n            font-size: clamp(0.56rem, 1.5vw, 0.72rem);\n            letter-spacing: 0.4em;\n            color: color-mix(in srgb, var(--accent) 65%, transparent);\n            margin-top: 0.4em;\n        }\n\n        @keyframes book-bob {\n\n            0%,\n            100% {\n                transform: translateY(0);\n            }\n\n            50% {\n                transform: translateY(-3.5%);\n            }\n        }\n\n        @keyframes book-floor {\n\n            0%,\n            100% {\n                transform: scaleX(1);\n                opacity: 0.85;\n            }\n\n            50% {\n                transform: scaleX(0.92);\n                opacity: 0.6;\n            }\n        }\n\n        @media (prefers-reduced-motion: reduce) {\n\n            [data-page=\"ai-home\"] .book-wrap,\n[data-page=\"ai-home\"] .book-floor-shadow {\n                animation: none;\n            }\n        }\n```\n\nReplace with nothing (delete it).\n\n- [ ] **Step 2: Remove the `ai-home-book-in` keyframe and `.book-scene` entrance animation**\n\nDelete line 1515 and line 1519. The exact strings to remove:\n\nLine 1515:\n```css\n        @keyframes ai-home-book-in { from { opacity: 0; transform: translateY(18px) scale(0.98); } to { opacity: 1; transform: none; } }\n```\n\nLine 1519:\n```css\n        [data-page=\"ai-home\"] .book-scene { animation: ai-home-book-in var(--dur-4) var(--ease-smooth) 120ms backwards; }\n```\n\nThe remaining motion rules (lines 1513-1518 minus 1515 and 1519) should look like:\n\n```css\n        /* ── Motion (spec 2026-08-14) ── */\n        @keyframes ai-home-rise { from { opacity: 0; transform: translateY(var(--rise)); } to { opacity: 1; transform: none; } }\n        [data-page=\"ai-home\"] .mo-in { animation: ai-home-rise var(--dur-4) var(--ease-smooth) backwards; }\n        [data-page=\"ai-home\"] .mo-d1 { animation-delay: var(--stagger); }\n        [data-page=\"ai-home\"] .mo-d2 { animation-delay: calc(var(--stagger) * 2); }\n```\n\n- [ ] **Step 3: Add hero video CSS**\n\nInsert the following after the apple-color-cycle dark-mode rule (after `--base-color: #ffffff; }`, before the `.bdh-neural-card` block). The insertion point is between the current `.dark [data-page=\"ai-home\"] .apple-color-cycle` block and the `[data-page=\"ai-home\"] .bdh-neural-card` block:\n\n```css\n\n        /* --- Hero Video --- */\n        [data-page=\"ai-home\"] .hero-section {\n            position: relative;\n            width: 100%;\n            height: 100dvh;\n            overflow: hidden;\n        }\n\n        [data-page=\"ai-home\"] .hero-video {\n            position: absolute;\n            inset: 0;\n            width: 100%;\n            height: 100%;\n            object-fit: cover;\n            opacity: 0;\n            transition: opacity 0.6s var(--ease-smooth);\n        }\n\n        [data-page=\"ai-home\"] .hero-video.loaded {\n            opacity: 1;\n        }\n\n        [data-page=\"ai-home\"] .hero-overlay {\n            position: absolute;\n            inset: 0;\n            background: linear-gradient(180deg,\n                rgba(0, 0, 0, 0.2) 0%,\n                rgba(0, 0, 0, 0.4) 100%);\n            pointer-events: none;\n        }\n\n        .dark [data-page=\"ai-home\"] .hero-overlay {\n            background: linear-gradient(180deg,\n                rgba(0, 0, 0, 0.35) 0%,\n                rgba(0, 0, 0, 0.55) 100%);\n        }\n\n        [data-page=\"ai-home\"] .hero-content {\n            position: absolute;\n            inset: 0;\n            z-index: 2;\n            display: flex;\n            flex-direction: column;\n            align-items: center;\n            justify-content: center;\n            text-align: center;\n            padding: 2rem;\n        }\n\n        [data-page=\"ai-home\"] .hero-text {\n            opacity: 0;\n            transition: opacity 0.3s var(--ease-smooth);\n        }\n\n        [data-page=\"ai-home\"] .hero-text.visible {\n            opacity: 1;\n        }\n\n        [data-page=\"ai-home\"] .hero-content .apple-color-cycle {\n            --base-color: rgba(255, 255, 255, 0.9);\n        }\n\n        [data-page=\"ai-home\"] .hero-replay {\n            position: absolute;\n            bottom: 2rem;\n            right: 2rem;\n            z-index: 3;\n            opacity: 0;\n            transition: opacity 0.3s var(--ease-smooth);\n            pointer-events: none;\n        }\n\n        [data-page=\"ai-home\"] .hero-replay.visible {\n            opacity: 1;\n            pointer-events: auto;\n        }\n```\n\n- [ ] **Step 4: Verify CSS has no dangling references**\n\n```bash\ngrep -n 'book-scene\\|book-wrap\\|book-leaf\\|book-cover\\|book-ribbon\\|book-page\\|leaf-face\\|book-floor\\|book-bob\\|ai-home-book-in\\|page-word\\|page-line\\|page-caption\\|page-doodle' src/site.css\n```\n\nExpected: no matches (all book references removed).\n\n---\n\n### Task 3: Rewrite hero HTML in AI/index.html\n\n**Files:**\n- Modify: `AI/index.html` (lines 89–148 replaced; new inline script added)\n\n- [ ] **Step 1: Remove the old two-column hero `<main>` block**\n\nDelete lines 89–148 (the `<main>` with `grid-cols-2` layout containing the left pane text + right pane storybook).\n\n- [ ] **Step 2: Insert the new video hero section**\n\nInsert after the opening `<body>` tag (before the existing Based AI `<section>`):\n\n```html\n\n    <!-- HERO: 3D Labs21 Logo Video -->\n    <section class=\"hero-section bg-parchment dark:bg-deep-dark\">\n        <video class=\"hero-video\"\n               src=\"video/labs21-hero.mp4\"\n               autoplay muted playsinline preload=\"auto\">\n        </video>\n        <div class=\"hero-overlay\"></div>\n\n        <div class=\"hero-content\">\n            <h1 class=\"text-7xl md:text-8xl lg:text-[7.5rem] font-serif tracking-tighter leading-[1.05] text-white\">\n                <span id=\"hero-stay\" class=\"hero-text block\">Stay</span>\n                <span id=\"hero-curious\" class=\"hero-text block mt-2 apple-color-cycle\">Curious.</span>\n            </h1>\n            <button id=\"hero-start\" class=\"hero-text skuo skuo-accent skuo-pill !px-7 !py-3 mt-8 group flex items-center gap-2\">\n                <span class=\"text-base font-bold\">Start here</span>\n                <svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" fill=\"currentColor\" viewBox=\"0 0 256 256\"\n                    class=\"transition-transform group-hover:translate-x-1\">\n                    <path\n                        d=\"M221.66,133.66l-72,72a8,8,0,0,1-11.32-11.32L200.69,136H40a8,8,0,0,1,0-16H200.69l-62.35-62.34a8,8,0,0,1,11.32-11.32l72,72A8,8,0,0,1,221.66,133.66Z\">\n                    </path>\n                </svg>\n            </button>\n        </div>\n\n        <button id=\"hero-replay\" class=\"hero-replay skuo skuo-icon w-11 h-11 rounded-full\" aria-label=\"Replay\">\n            <i class=\"fa-solid fa-rotate-right\"></i>\n        </button>\n    </section>\n```\n\n- [ ] **Step 3: Insert the hero video script**\n\nInsert after the hero section, before the Based AI `<section>`:\n\n```html\n    <script>\n        (function () {\n            const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n            if (reduceMotion) {\n                document.getElementById('hero-stay').classList.add('visible');\n                document.getElementById('hero-curious').classList.add('visible');\n                document.getElementById('hero-start').classList.add('visible');\n                return;\n            }\n\n            const video = document.querySelector('.hero-video');\n            if (!video) return;\n\n            const stayEl = document.getElementById('hero-stay');\n            const curiousEl = document.getElementById('hero-curious');\n            const startBtn = document.getElementById('hero-start');\n            const replayBtn = document.getElementById('hero-replay');\n            let complete = false;\n            let fired = { stay: false, curious: false, start: false };\n\n            video.addEventListener('canplay', function () {\n                video.classList.add('loaded');\n                video.play().catch(function () {});\n            });\n\n            video.addEventListener('timeupdate', function () {\n                var t = video.currentTime;\n                if (t >= 2 && !fired.stay)   { stayEl.classList.add('visible');    fired.stay = true; }\n                if (t >= 4.5 && !fired.curious) { curiousEl.classList.add('visible'); fired.curious = true; }\n                if (t >= 6.5 && !fired.start) { startBtn.classList.add('visible');  fired.start = true; }\n            });\n\n            video.addEventListener('ended', function () {\n                complete = true;\n                replayBtn.classList.add('visible');\n            });\n\n            replayBtn.addEventListener('click', function () {\n                complete = false;\n                fired = { stay: false, curious: false, start: false };\n                stayEl.classList.remove('visible');\n                curiousEl.classList.remove('visible');\n                startBtn.classList.remove('visible');\n                replayBtn.classList.remove('visible');\n                video.currentTime = 0;\n                video.play().catch(function () {});\n            });\n        })();\n    </script>\n```\n\n- [ ] **Step 4: Verify the HTML structure**\n\nCheck that the body contains, in order:\n1. Hero `<section>` (new)\n2. Hero video `<script>` (new)\n3. Based AI `<section>` (existing, untouched)\n4. BDH Neural `<section>` (existing, untouched)\n5. `NAV_CONFIG` script (existing, untouched)\n6. `<script src=\"../src/motion.js\">` (existing, untouched)\n7. `<script src=\"../src/nav.js\">` (existing, untouched)\n\nOpen the file and confirm the `</body>` closing tag is present and the structure is valid.\n\n---\n\n### Task 4: Verify in browser and run tests\n\n- [ ] **Step 1: Open the page and check video playback**\n\nOpen `AI/index.html` in a browser (file:// or via local server). Expected:\n- Video loads and auto-plays (muted)\n- \"Stay\" fades in at ~2s\n- \"Curious.\" with rainbow gradient fades in at ~4.5s\n- \"Start here\" button fades in at ~6.5s\n- Video freezes on final frame\n- Replay button (⟳) appears in bottom-right\n- Clicking replay restarts the sequence\n\n- [ ] **Step 2: Check dark mode**\n\nToggle dark mode. Expected:\n- Overlay is slightly darker (0.35→0.55 vs 0.2→0.4)\n- \"Curious.\" text gradient white-based (via `--base-color` override)\n- Hero section background is dark before video loads\n\n- [ ] **Step 3: Check reduced motion**\n\nEnable `prefers-reduced-motion: reduce` in browser dev tools. Reload. Expected:\n- Video does not play\n- All text and button visible immediately\n- No replay button\n\n- [ ] **Step 4: Check mobile responsive**\n\nResize to 375px width. Expected:\n- Video still covers full height (`100dvh`)\n- Text sizes scale down (via Tailwind `md:`/`lg:` breakpoints)\n- Start button and replay button are tappable\n- Content below hero scrolls normally\n\n- [ ] **Step 5: Run existing test suite** (if any applicable)\n\n```bash\nnode test-z-index.mjs\n```\n\nExpected: PASS (no z-index regressions from hero CSS changes).\n\n---\n\n## Self-Review Notes\n\nChecklist before declaring done:\n1. `grep -n 'book-scene\\|book-wrap\\|book-leaf\\|book-cover\\|book-ribbon\\|book-page\\|leaf-face\\|book-floor\\|book-bob\\|ai-home-book-in\\|page-word\\|page-line\\|page-caption\\|page-doodle' src/site.css` returns no matches\n2. `AI/video/labs21-hero.mp4` exists and is 15–25MB\n3. `AI/index.html` no longer contains storybook markup or page-turn script\n4. Hero shows video, text reveals fire in order, replay works\n5. Dark mode, mobile, reduced-motion all verified\n"
}
