{
  "filePath": "/Users/ar12c/Desktop/web/docs/superpowers/specs/2026-08-26-labs21-hero-video-design.md",
  "content": "# Labs21 3D Logo Hero Video — Design Spec\n\n**Date:** 2026-08-26\n\n## Summary\n\nReplace the two-column hero (\"Stay Curious\" + flipping storybook) on `AI/index.html` with a full-width, auto-playing video hero: the 288-frame 3D Labs21 logo sequence encoded as mp4, with text overlays that fade in at choreographed moments during playback.\n\n---\n\n## Source Material\n\n- 288 PNGs at `/Users/ar12c/Desktop/Labs21/0001.png` through `0288.png`\n- 1920×1080, RGBA, ~2MB each (~576MB raw total)\n- The climax (logo becomes visible) occurs between frames 151–250\n\n---\n\n## Approach: Video + timeupdate sync\n\nConvert the PNG sequence to an mp4 video (~15MB, 30fps, ~9.6s). Use a `<video>` element as the hero background. Text reveals sync to playback timestamps via the `timeupdate` event.\n\n### Why video over canvas\n\n| | Video | Canvas |\n|---|---|---|\n| File size | ~15MB | ~80MB |\n| Decode | Hardware-accelerated | Software (CPU) |\n| Mobile perf | Smooth | Slow, memory-heavy |\n| Sync precision | ~250ms (timeupdate) | Frame-perfect |\n| Implementation | Simple | Complex |\n\n`timeupdate` granularity (~250ms) is fine because text fades in over 300ms transitions — the fade masks any imprecision.\n\n---\n\n## Hero Layout\n\n```\n┌──────────────────────────────────────────────┐\n│                                              │\n│          <video> full-width cover            │\n│                                              │\n│   ┌──────────────────────────────────┐       │\n│   │    dark gradient overlay         │       │\n│   │                                  │       │\n│   │         \"Stay\"                   │       │\n│   │         \"Curious.\"               │       │\n│   │    (apple-color-cycle gradient)  │       │\n│   │                                  │       │\n│   │       [ Start here ]             │       │\n│   │                                  │       │\n│   │                        [⟳]      │       │\n│   └──────────────────────────────────┘       │\n│                                              │\n└──────────────────────────────────────────────┘\n```\n\n- **Video**: `object-fit: cover`, `width: 100%`, `height: 100vh` (or `100dvh`)\n- **Overlay**: dark gradient (`rgba(0,0,0,0.2)` top → `rgba(0,0,0,0.4)` bottom in light; `0.35→0.55` in dark) for text readability\n- **Text container**: centered, z-indexed above video, flex column\n- **Dimensions**: full viewport height hero, no scroll below the fold before content sections\n\n---\n\n## Text Choreography\n\n| Time | Frame (~30fps) | Event |\n|---|---|---|\n| 0s | 1 | Video plays, text area empty |\n| 2s | 60 | **\"Stay\"** fades in (0.3s transition) |\n| 4.5s | 135 | **\"Curious.\"** fades in below (0.3s transition) |\n| 6.5s | 195 | **\"Start here\"** button fades in (0.3s transition) |\n| 9.6s | 288 | Video pauses on final frame. **Replay button** fades in (bottom-right corner) |\n\n- \"Curious.\" uses the existing `.apple-color-cycle` gradient-text animation\n- The Start button uses the existing `.skuo skuo-accent skuo-pill` classes\n- Replay button: small round icon button (Font Awesome `fa-rotate-right` or similar), bottom-right, semi-transparent, restarts video + hides text → replays sequence\n\n### States\n\n- **Loading**: Hero shows a solid background (matches current `bg-parchment`/`deep-dark`) until video `canplay` fires — then the video fades in\n- **Playing**: text reveals fire at timestamps above\n- **Complete**: video paused on frame 288, replay button visible\n- **Replay**: replay button clicked → text hides (opacity 0), video restarts from 0, choreography repeats\n\n---\n\n## Fallbacks\n\n- **`prefers-reduced-motion`**: video hidden, hero shows static background with all text and button visible immediately (no fade sequence)\n- **Mobile (≤768px)**: same layout, smaller text sizing via existing responsive classes\n- **Video fails to load**: fall back to a static background + all text/button visible (no-op, the hero just shows the content)\n- **Autoplay blocked** (rare on modern browsers with `muted`): show a click-to-play overlay with a play icon\n\n---\n\n## Files Changed\n\n### `AI/index.html`\n\n- **Remove**: the two-column `<main>` grid hero (lines ~89–148), including the left-pane text, right-pane storybook markup, and the `<script>` that drives the page-turn animation\n- **Add**: new hero `<section>` with `<video>`, overlay `<div>`, text container (`<h1>` + `<button>`), replay button\n- **Add**: new inline `<script>` for video-timeupdate text choreography + replay logic\n- **Keep**: everything below the hero — Based AI pillars, BDH Neural section, NAV_CONFIG, script tags\n\n### `src/site.css`\n\n- **Remove**: the entire `.book-scene`, `.book-wrap`, `.book`, `.book-cover`, `.book-ribbon`, `.book-page`, `.book-leaf`, `.leaf-face`, `.book-floor-shadow`, `.page-word`, `.page-line`, `.page-caption`, `.page-doodle` CSS blocks, and the `@keyframes book-bob` / `book-floor` animations (~200 lines in the `[data-page=\"ai-home\"]` section)\n- **Remove**: `.book-scene` entrance animation rule (`animation: ai-home-book-in ...`)\n- **Add**: `.hero-video`, `.hero-overlay`, `.hero-text`, `.hero-replay` styles in the `[data-page=\"ai-home\"]` section\n- **Keep**: `.apple-color-cycle`, `.mo-in`/`.mo-d*` (reused for the new text reveals), `.bdh-*` styles, `@keyframes apple-gradient-slide`, `@keyframes ai-home-rise`\n\n### New file: `AI/video/labs21-hero.mp4`\n\nGenerated via ffmpeg from the 288 PNGs. Command:\n\n```bash\nffmpeg -framerate 30 -i /Users/ar12c/Desktop/Labs21/%04d.png \\\n  -c:v libx264 -preset slow -crf 23 -pix_fmt yuv420p \\\n  -movflags +faststart \\\n  AI/video/labs21-hero.mp4\n```\n\n- Output: ~15–25MB, H.264, 30fps, yuv420p (universal compatibility)\n- `+faststart` moves the moov atom to the front for streaming\n- No audio track\n\n---\n\n## CSS Details\n\n```css\n[data-page=\"ai-home\"] .hero-video {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  object-fit: cover;\n  opacity: 0;\n  transition: opacity 0.6s var(--ease-smooth);\n}\n\n[data-page=\"ai-home\"] .hero-video.loaded {\n  opacity: 1;\n}\n\n[data-page=\"ai-home\"] .hero-overlay {\n  position: absolute;\n  inset: 0;\n  background: linear-gradient(180deg,\n    rgba(0,0,0,0.2) 0%,\n    rgba(0,0,0,0.4) 100%);\n  pointer-events: none;\n}\n\n.dark [data-page=\"ai-home\"] .hero-overlay {\n  background: linear-gradient(180deg,\n    rgba(0,0,0,0.35) 0%,\n    rgba(0,0,0,0.55) 100%);\n}\n\n[data-page=\"ai-home\"] .hero-text {\n  opacity: 0;\n  transition: opacity 0.3s var(--ease-smooth);\n}\n\n[data-page=\"ai-home\"] .hero-text.visible {\n  opacity: 1;\n}\n\n[data-page=\"ai-home\"] .hero-replay {\n  position: absolute;\n  bottom: 2rem;\n  right: 2rem;\n  opacity: 0;\n  transition: opacity 0.3s var(--ease-smooth);\n  pointer-events: none;\n}\n\n[data-page=\"ai-home\"] .hero-replay.visible {\n  opacity: 1;\n  pointer-events: auto;\n}\n```\n\n---\n\n## JS Logic (pseudocode)\n\n```js\nconst video = document.querySelector('.hero-video');\nconst stayEl = document.getElementById('hero-stay');\nconst curiousEl = document.getElementById('hero-curious');\nconst startBtn = document.getElementById('hero-start');\nconst replayBtn = document.getElementById('hero-replay');\nlet complete = false;\nlet fired = { stay: false, curious: false, start: false };\n\nvideo.addEventListener('canplay', () => {\n  video.classList.add('loaded');\n  video.play();\n});\n\nvideo.addEventListener('timeupdate', () => {\n  const t = video.currentTime;\n  if (t >= 2 && !fired.stay)   { stayEl.classList.add('visible');    fired.stay = true; }\n  if (t >= 4.5 && !fired.curious) { curiousEl.classList.add('visible'); fired.curious = true; }\n  if (t >= 6.5 && !fired.start) { startBtn.classList.add('visible');  fired.start = true; }\n});\n\nvideo.addEventListener('ended', () => {\n  complete = true;\n  replayBtn.classList.add('visible');\n});\n\nreplayBtn.addEventListener('click', () => {\n  complete = false;\n  fired = { stay: false, curious: false, start: false };\n  [stayEl, curiousEl, startBtn, replayBtn].forEach(el => el.classList.remove('visible'));\n  video.currentTime = 0;\n  video.play();\n});\n\n// prefers-reduced-motion: skip video, show all content\nif (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {\n  video.remove();\n  [stayEl, curiousEl, startBtn].forEach(el => el.classList.add('visible'));\n}\n```\n\n---\n\n## What stays the same\n\n- \"Based AI\" pillars section\n- \"BDH Neural Architecture\" section\n- `NAV_CONFIG` and script tags (nav.js, motion.js)\n- Tailwind CDN + config\n- `src/site.css` link\n- Theme anti-FOUC script\n- Font declarations\n- `.apple-color-cycle` gradient text effect (reused on \"Curious.\")\n- `.mo-in` / `.mo-d*` entrance animation classes (reused for text reveals if desired, or replaced by the timeupdate-driven `.visible` toggles)\n- `[data-page=\"ai-home\"]` data attribute on body\n\n---\n\n## What goes away\n\n- Two-column grid hero `<main>`\n- Storybook markup: `.book-scene`, `.book-wrap`, `.book`, `.book-cover`, `.book-ribbon`, `.book-page--left`, `.book-page--right`, `.book-leaf`, `.leaf-face--front`, `.leaf-face--back`, `.book-floor-shadow`\n- Storybook CSS: ~200 lines in `src/site.css` `[data-page=\"ai-home\"]` section\n- Storybook JS: the `pageTurnLoop()` async function and WAAPI animation code\n- Keyframes: `@keyframes book-bob`, `@keyframes book-floor`, `@keyframes ai-home-book-in`\n- The `FUN_WORDS` array and related book-turn logic\n\n---\n\n## Edge cases\n\n1. **Browser caches video on revisit** — no loading flash, `canplay` fires fast\n2. **Tab hidden during playback** — video continues in background (muted, no audio concern); text reveals may fire unseen but state is correct when user returns\n3. **Resize during playback** — `object-fit: cover` handles this naturally\n4. **Slow connection** — video loads progressively via `+faststart`; hero shows background color until loaded\n5. **Safari autoplay** — `muted` + `playsinline` satisfies Safari's autoplay policy\n"
}
